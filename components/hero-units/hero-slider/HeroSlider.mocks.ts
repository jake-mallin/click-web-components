import { IHeroSlider } from './HeroSlider';

const base: IHeroSlider = {
  heroMainHeading: 'Welcome to Jakes Cars',
  // heroSubHeading: 'Quality used cars in Stoke-on-Trent',
};

export const mockHeroSliderProps = {
  base,
};
