import { IHeroSearch } from './HeroSearch';

const base: IHeroSearch = {
  heroMainHeading: 'Welcome to Jakes Cars',
  heroSubHeading: 'Quality used cars in Stoke-on-Trent',
};

export const mockHeroSearchProps = {
  base,
};
